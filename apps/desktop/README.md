# Desktop

The desktop surface is planned as a local-first shell around Hermes-class workflows, memory inspection, and offline experiment control. The current repository keeps the shared runtime ready for that application layer.

