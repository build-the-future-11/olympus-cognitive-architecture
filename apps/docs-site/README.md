# Docs Site

The initial documentation is Markdown-first under `/docs`. This directory is reserved for a future generated docs site once the API, behavior language, and model-card set expand further.

