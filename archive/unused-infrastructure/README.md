# Archived infrastructure scaffold

This PostgreSQL/pgvector and Redis scaffold was removed from the active project
surface because no Olympus package imports a client, defines a schema, or uses
either service. It is retained here only as provenance. Moving it back requires
an implemented consumer, integration tests, and documented ownership.
