# Stage 03 — Foundry Integrity Hardening

**State: OUTCOME_VERIFIED.** A fresh root registered immutable data, trained a
real bigram checkpoint, beat its frozen baseline, exported, served, restarted,
and recovered with registry integrity `ok`. Tamper rejection and resume
configuration identity are automated tests. Clean-wheel replay also returned
one dataset, experiment, checkpoint, evaluation, model, and five evidence
events.

Claim boundary: this verifies lifecycle integrity, not assistant capability.
